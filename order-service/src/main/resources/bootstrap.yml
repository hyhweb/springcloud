spring:
  application:
    name: order-service

  cloud:
    nacos:
      config:
        server-addr: 192.168.226.128:8848
        file-extension: yml
      discovery:
        server-addr: 192.168.226.128:8848
  boot:
    admin:
      client:
        url: http://localhost:8070
#        username: admin
#        password: admin

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS